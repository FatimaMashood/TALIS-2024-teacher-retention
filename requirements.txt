# Analysis dependencies for analysis.py
# Pinned to minimum versions known to support the APIs used
# (statsmodels.OrderedModel requires >= 0.13; tested on the 2025/2026 stable line).
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
matplotlib>=3.7
